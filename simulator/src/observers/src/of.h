#ifndef OF_H
#define OF_H

// template <typename T = void *>
// auto of() -> LiftedObservable<T> *;

// template <typename T, typename... Rest>
// auto of(const T &value, const Rest &...rest) -> LiftedObservable<T> *;

// #include "of.tpp"

#endif