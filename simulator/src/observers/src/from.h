#ifndef FROM_H
#define FROM_H

// template <typename T, typename A, template <class, class> class Container>
// auto from(Container<T, A> &&value) ->
// LiftedObservable<decltype(*value.begin())> *;

// #include "from.tpp"

#endif