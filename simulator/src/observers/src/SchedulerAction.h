#ifndef SCHEDULER_ACTION_H
#define SCHEDULER_ACTION_H

#include <TkArch/Observers.h>

// template <typename T> class SchedulerAction : public Subscription {
// public:
//   Subscription schedule(const T &state) = 0;

//   Subscription schedule(const T &state, long int delayMs) = 0;
// };

#endif